import { StyleSheet } from 'react-native';

export const sharedStyles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  fieldBorder: {
    borderWidth: 1.5,
    borderRadius: 12,
  },
  centeredButton: {
    height: 52,
    borderRadius: 14,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
